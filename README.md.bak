<<<<<<< HEAD
# ESP32 智能工位监控系统

基于 ESP32-S3 的智能工位环境监测与控制系统，集成多传感器数据采集、RFID门禁控制、OLED显示和MQTT云端通信功能。

## 📋 项目概述

本项目实现了一个完整的智能工位管理系统，通过RFID卡片控制工位设备的开关，实时监测环境参数（温湿度、光照、烟雾、火焰等），并通过MQTT协议将数据上传至云平台。**完全兼容OneNet物模型标准**。

### 核心功能

- 🔐 **RFID门禁控制**：刷卡上班/下班，自动开关工位电源和继电器
- 🌡️ **环境监测**：温湿度、光照强度、烟雾浓度、火焰检测、热度监测
- 📡 **MQTT云端通信**：实时上传传感器数据到物联网平台（OneNet标准）
- 🖥️ **OLED显示**：本地显示温湿度和WiFi状态
- ⚡ **继电器控制**：支持多路继电器设备控制
- 🌐 **远程控制**：支持云端远程开关设备和电源管理
- 📊 **无人值守监控**：未刷卡时定期上报基础传感器数据
- 🚨 **紧急断电保护**：传感器异常时自动断电并上报紧急事件

---

## 🔧 硬件配置

### 开发板
- **主控芯片**：ESP32-S3

### 传感器模块（6个）

| 传感器类型 | GPIO引脚 | ADC通道 | 功能描述 |
|----------|---------|---------|---------|
| DHT11温湿度传感器 | GPIO 26 | - | 测量环境温度和湿度 |
| 热敏传感器 | GPIO 34 | ADC1_CH6 | 检测环境热度 |
| 光敏传感器 | GPIO 35 | ADC1_CH7 | 测量光照强度 |
| 火焰传感器 | GPIO 32 | ADC1_CH4 | 检测火焰信号 |
| 烟雾传感器 | GPIO 33 | ADC1_CH5 | 检测烟雾浓度 |
| 人体红外传感器 | GPIO 36 | - | 检测人体移动 |

### 执行器（4路继电器）

| 执行器名称 | GPIO引脚 | 功能描述 |
|----------|---------|---------|
| 电源控制 | GPIO 0 | 工位总电源开关 |
| 灯光控制 | GPIO 2 | 照明灯开关 |
| 1号继电器 | GPIO 13 | 扩展设备1 |
| 2号继电器 | GPIO 4 | 扩展设备2 |

### RFID读卡器（RC522）

| 信号 | ESP32引脚 | 说明 |
|-----|----------|-----|
| SPI MISO | GPIO 25 | SPI数据输入 |
| SPI MOSI | GPIO 21 | SPI数据输出 |
| SPI SCLK | GPIO 22 | SPI时钟 |
| SPI CS | GPIO 5 | 片选信号 |
| RST | -1 | 软复位（不使用物理引脚） |

### OLED显示屏（SSD1306）

| 信号 | ESP32引脚 | 说明 |
|-----|----------|-----|
| I2C SDA | GPIO 23 | I2C数据线 |
| I2C SCL | GPIO 18 | I2C时钟线 |
| 地址 | 0x3C | I2C设备地址 |

---

## ✨ 功能特性

### 1. RFID门禁控制

- **刷卡上班**：将MIFARE卡片靠近RC522读卡器
  - 自动读取卡片Block 1的前10个字符作为用户ID
  - 打开所有继电器（电源、灯光、1号、2号）
  - 用户ID上传至MQTT服务器

- **刷卡下班**：移除卡片
  - 关闭所有继电器
  - 用户ID设置为"null"并上传

### 2. 环境数据采集

- **采样周期**：5秒
- **ADC配置**：12位精度，0-3.3V量程
- **数据类型**：
  - 温度：摄氏度（℃）
  - 湿度：百分比（%）
  - 光照/烟雾/火焰/热度：百分比（0-100%）

### 3. MQTT云端通信（OneNet标准）

- **Broker地址**：`mqtt://218.201.45.2:1883`
- **产品ID**：`3HSBa0ZB1R`
- **设备ID**：`ESP_01`

#### 📤 发布主题（设备→云端）
- **属性上报**：`$sys/3HSBa0ZB1R/ESP_01/thing/property/post`
- **事件上报**：`$sys/3HSBa0ZB1R/ESP_01/thing/event/post`

#### 📥 订阅主题（云端→设备）
- **属性设置**：`$sys/3HSBa0ZB1R/ESP_01/thing/property/set`
- **事件回复**：`$sys/3HSBa0ZB1R/ESP_01/thing/event/post/reply`

#### 🎯 事件类型
- **AlertEmergency**：紧急告警（火灾、烟雾、高温等）
- **AlertWarning**：一般告警（温度越限等）
- **EmergencyPowerOff**：紧急断电通知

### 4. 远程控制功能

- **云端远程控制**：支持远程开关设备和电源
- **智能用户名管理**：
  - 远程开启电源时用户名设为"0000000001"（管理员）
  - 远程关闭电源时用户名重置为"null"
  - 用户刷卡时用户名更新为刷卡用户

### 5. 无人值守监控

- **基础传感器上报**：未刷卡时每30秒上报温湿度、烟雾数据
- **完整数据上报**：刷卡时每60秒上报所有传感器和设备状态
- **智能切换**：刷卡后自动切换到完整数据上报模式

### 6. 紧急断电保护

- **自动断电**：传感器异常时自动切断电源
- **事件通知**：发送EmergencyPowerOff和AlertEmergency事件
- **安全恢复**：远程恢复供电时清除紧急状态标志

### 7. WiFi连接

- **模式**：STA（Station）模式
- **SSID**：`pc_lk`
- **密码**：`123456789`
- **自动重连**：支持

---

## 📊 数据格式（OneNet标准）

### 属性数据JSON格式（完整上报）

```json
{
  "id": "123",
  "version": "1.0",
  "params": {
    "CurrentTemperature": {"value": 25.5},
    "FlameScope": {"value": 10.2},
    "HeatScope": {"value": 45.8},
    "LightLuxValue": {"value": 78.3},
    "LightStatus": {"value": 1},
    "PowerStatus": {"value": 1},
    "RelativeHumidity": {"value": 60.5},
    "RelayNum1Status": {"value": 1},
    "RelayNum2Status": {"value": 1},
    "SmokeScope": {"value": 5.2},
    "username": {"value": "ABC1234567"}
  }
}
```

### 基础传感器数据JSON格式（未刷卡时）

```json
{
  "id": "123",
  "version": "1.0",
  "params": {
    "CurrentTemperature": {"value": 25.5},
    "RelativeHumidity": {"value": 60.5},
    "SmokeScope": {"value": 5.2}
  }
}
```

### 事件数据JSON格式（OneNet标准）

```json
{
  "id": "123",
  "version": "1.0",
  "params": {
    "AlertEmergency": {
      "value": {
        "alertType": "FIRE_EMERGENCY",
        "severity": "EMERGENCY",
        "sensorValue": 75.0,
        "threshold": 70.0,
        "timestamp": "2023-10-27T08:30:00Z",
        "username": "ABC1234567",
        "temperature": 25.5,
        "humidity": 60.5,
        "flameScope": 75.0,
        "smokeScope": 5.2
      }
    }
  }
}
```

### 数据字段说明

#### 属性字段
| 字段名 | 类型 | 单位 | 说明 |
|-------|------|-----|------|
| CurrentTemperature | float | ℃ | 当前温度 |
| RelativeHumidity | float | % | 相对湿度 |
| LightLuxValue | float | % | 光照强度（百分比） |
| SmokeScope | float | % | 烟雾浓度 |
| FlameScope | float | % | 火焰强度 |
| HeatScope | float | % | 热度 |
| PowerStatus | int | 0/1 | 电源状态（0关/1开） |
| LightStatus | int | 0/1 | 灯光状态 |
| RelayNum1Status | int | 0/1 | 1号继电器状态 |
| RelayNum2Status | int | 0/1 | 2号继电器状态 |
| username | string | - | 用户名 |

#### 事件字段
| 字段名 | 类型 | 说明 |
|-------|------|------|
| alertType | string | 告警类型（FIRE_EMERGENCY等） |
| severity | string | 严重程度（EMERGENCY/WARNING） |
| sensorValue | float | 触发告警的传感器值 |
| threshold | float | 告警阈值 |
| timestamp | string | 告警时间（ISO 8601格式） |
| username | string | 触发告警的用户 |
| temperature | float | 当时温度 |
| humidity | float | 当时湿度 |
| flameScope | float | 当时火焰强度 |
| smokeScope | float | 当时烟雾浓度 |

---

## 🚀 快速开始

### 环境要求

- **ESP-IDF版本**：>= 4.1.0
- **工具链**：ESP-IDF工具链
- **Python**：>= 3.6

### 安装步骤

1. **克隆项目**
   ```bash
   git clone <repository_url>
   cd test1_demo
   ```

2. **配置WiFi和MQTT**
   
   修改 `main/inc/config.h`：
   ```c
   #define ESP_WIFI_STA_SSID "your_wifi_ssid"
   #define ESP_WIFI_STA_PASSWD "your_wifi_password"
   ```
   
   修改 `main/main.c` 中的MQTT配置（如需更改）。

3. **编译项目**
   ```bash
   idf.py build
   ```

4. **烧录固件**
   ```bash
   idf.py -p COM3 flash
   ```
   > 注：将 `COM3` 替换为你的串口号

5. **查看日志**
   ```bash
   idf.py -p COM3 monitor
   ```

---

## 📝 使用说明

### RFID卡片准备

1. **卡片类型**：MIFARE Classic 1K/4K 或兼容卡片
2. **写入用户ID**：
   - 使用NFC读写工具
   - 在卡片的**Block 1**写入用户ID（10个字符）
   - 例如：`USER123456`（不足10位会自动补齐）
3. **认证密钥**：使用默认密钥 `FF FF FF FF FF FF`

### 系统启动流程

1. 上电后自动初始化所有外设
2. 连接WiFi热点
3. 连接MQTT服务器并订阅控制主题
4. 初始化RC522读卡器
5. 开始传感器数据采集（5秒周期）

### 工作流程

```
┌─────────────────────────────────────────────────┐
│  系统启动 → WiFi连接 → MQTT连接 → RC522初始化     │
└─────────────────────────────────────────────────┘
                          ↓
             ┌─────────────────────────┐
             │   未刷卡（初始状态）      │
             │  • 所有设备关闭           │
             │  • username = "null"     │
             │  • 30秒上报温湿度和烟雾    │
             └─────────────────────────┘
                          ↓
┌──────────────┐     【刷卡靠近】     ┌──────────────┐
│   远程控制   │ ←────────────────→ │   用户刷卡   │
│ • 可远程开关设备 │                 │ • username更新 │
│ • 管理员用户名管理 │                 │ • 设备自动开启 │
└──────────────┘                     └──────────────┘
                          ↓
             ┌─────────────────────────┐
             │    正常工作状态         │
             │  • 每60秒采集所有传感器   │
             │  • MQTT上传完整数据      │
             │  • 自动控制功能启用       │
             └─────────────────────────┘
                          ↓
              【传感器异常】          【移除卡片】
                    ↓                       ↓
             ┌──────────────┐     ┌──────────────┐
             │ 紧急断电保护  │     │ 下班离开      │
             │ • 自动切断电源 │     │ • 关闭所有设备 │
             │ • 发送紧急事件 │     │ • username=null │
             └──────────────┘     └──────────────┘
```

---

## 🗂️ 项目结构

```
test1_demo/
├── main/
│   ├── main.c                 # 主程序（新增远程控制、无人值守监控）
│   ├── headfile.h             # 头文件汇总
│   ├── inc/
│   │   ├── config.h           # 配置文件
│   │   ├── rtos_task.h        # RTOS任务定义
│   │   └── icon_codeing.h     # OLED图标编码
│   └── src/
│       ├── config.c           # 配置实现
│       └── rtos_task.c        # RTOS任务实现
├── components/
│   ├── led/                   # LED控制组件
│   └── key/                   # 按键驱动组件
├── managed_components/        # ESP组件管理器依赖
│   ├── abobija__rc522/        # RC522 RFID驱动
│   ├── espressif__ssd1306/    # OLED显示驱动
│   ├── esp-idf-lib__dht/      # DHT11温湿度驱动
│   └── ...
├── ONENET_TOPIC_GUIDE.md      # 📄 OneNet主题使用指南（新增）
├── EDGE_DETECTION_README.md   # 📄 边缘检测系统说明（已更新）
├── BACKEND_INTEGRATION_EXAMPLE.md # 📄 后端集成示例（已更新）
├── CHANGES_SUMMARY.md         # 📄 更新摘要（新增）
├── CMakeLists.txt             # CMake配置
├── sdkconfig                  # ESP-IDF配置
└── README.md                  # 本文件（已更新）
```

---

## 📦 依赖组件

本项目使用ESP-IDF组件管理器管理以下依赖：

```yaml
dependencies:
  idf: ">=4.1.0"
  espressif/ds18b20: "^0.2.0"
  espressif/led_strip: "^3.0.1~1"
  espressif/ssd1306: "^1.0.5~1"
  esp-idf-lib/dht: "^1.1.7"
  espp/adc: "^1.0.25"
  abobija/rc522: "^3.4.3"
  espressif/onewire_bus: "^2.0.0"    # 新增：OneWire总线支持
  espressif/led_strip: "^3.0.1~1"   # LED灯带控制
  espp/format: "^1.0.0"             # 新增：格式化输出支持
  espp/logger: "^1.0.0"             # 新增：日志系统增强
  espp/task: "^1.0.0"               # 新增：任务管理增强
```

## 📋 新增特性说明

### 🌐 OneNet标准兼容
- **完全兼容OneNet物模型标准**
- **支持OneNet规则引擎**
- **可在OneNet控制台直接调试**

### 📡 远程控制功能
- **云端远程开关设备**
- **智能用户名管理**
- **远程紧急断电恢复**

### 📊 无人值守监控
- **未刷卡时定期上报基础数据**
- **智能切换上报频率**
- **资源节约型监控**

### 🚨 紧急保护系统
- **多传感器异常检测**
- **自动断电保护**
- **双重事件通知机制**

---

## ⚙️ 配置说明

### WiFi配置

在 `main/inc/config.h` 中修改：

```c
#define ESP_WIFI_STA_SSID "your_ssid"       // WiFi名称
#define ESP_WIFI_STA_PASSWD "your_password" // WiFi密码
```

### MQTT配置

在 `main/main.c` 的 `mqtt_app_init()` 函数中修改：

```c
esp_mqtt_client_config_t mqtt_cfg = {
    .broker.address.uri = "mqtt://your_broker:1883",
    .credentials.username = "your_username",
    .credentials.client_id = "your_client_id",
    .credentials.authentication.password = "your_password"
};
```

### RC522引脚配置

在 `main/main.c` 中修改：

```c
#define RC522_SPI_BUS_GPIO_MISO 25
#define RC522_SPI_BUS_GPIO_MOSI 21
#define RC522_SPI_BUS_GPIO_SCLK 22
#define RC522_SPI_SCANNER_GPIO_SDA 5
#define RC522_SCANNER_GPIO_RST -1  // -1表示软复位
```

### 新功能配置

#### 远程控制功能
- **自动启用**：远程控制指令自动解析并执行
- **权限管理**：远程开启电源时自动设为管理员身份
- **状态同步**：远程操作后状态变量自动更新

#### 无人值守监控
- **基础上报间隔**：30秒（未刷卡时）
- **完整上报间隔**：60秒（刷卡时）
- **自动切换**：刷卡后自动切换到完整数据上报模式

#### 紧急断电保护
- **自动检测**：多传感器异常自动触发
- **双重通知**：同时发送EmergencyPowerOff和AlertEmergency事件
- **安全恢复**：远程恢复供电时自动清除紧急状态

---

## 🐛 调试说明

### 查看日志

系统日志包含以下标签：

- **WIFI**：WiFi连接状态
- **MQTT**：MQTT通信日志（含远程控制）
- **ENV**：环境传感器数据
- **RC522**：RFID读卡日志
- **ALERT**：自动控制和告警日志

### 正常运行日志示例

```
I (1234) WIFI: Successfully connected to ap SSID:pc_lk
I (1235) WIFI: Obtained IP address: 192.168.1.100
I (2000) MQTT: MQTT_EVENT_CONNECTED
I (2001) RC522: RC522 RFID reader initialized successfully
I (2002) MQTT: Subscribed to property set, msg_id=...
I (2003) MQTT: [OK] MQTT fully connected and subscribed to all topics
I (5000) ENV: 温湿度：湿度 60.5% | 温度 25.50℃ | 火焰 10 | 光照 78 | 烟雾 5 | 热度 45
I (5030) ALERT: Basic sensor upload (30s timer, no card)
I (5100) RC522: Card detected! User ID from Block 1: USER123456
I (5101) MQTT: Remote power on by administrator: 0000000001
I (5102) RC522: Power and relays turned ON
I (5200) MQTT: Generated JSON: {"id":"123",...}
```

### 远程控制日志示例

```
I (MQTT) Processing remote control command
I (MQTT) Received command: {"LightStatus":1,"PowerStatus":1,"RelayNum1Status":1}
I (MQTT) Setting light status to: 1
I (MQTT) Setting power status to: 1
I (MQTT) Setting relay1 status to: 1
I (MQTT) Remote control command processed successfully
```

### 自动控制日志示例

```
I (ALERT) Auto control check - Power: 1, Emergency: 0, Light: 15.0%, Smoke: 45.0%
I (ALERT) Auto light control: ON (Light intensity: 15.0%, threshold: 30.0%)
I (ALERT) Auto relay1 control: OFF (Smoke intensity: 45.0%, threshold: 50.0%)
```

### 紧急断电日志示例

```
I (ALERT) [SMOKE EMERGENCY] Smoke: 75.0% (Threshold: 95.0%)
I (ALERT) Auto power-off triggered by smoke emergency!
I (ALERT) EMERGENCY POWER OFF TRIGGERED!
I (MQTT) Sending EmergencyPowerOff event to OneNet
I (MQTT) Sending AlertEmergency event to OneNet
I (ALERT) [EMERGENCY MODE] Device powered off for safety!
```

### 常见问题

**Q1: RC522无法读取卡片**
- 检查SPI引脚连接是否正确
- 确认卡片类型为MIFARE Classic
- 检查卡片是否使用默认密钥（FF FF FF FF FF FF）

**Q2: MQTT无法连接**
- 检查WiFi是否连接成功
- 确认MQTT服务器地址和端口正确
- 检查用户名和密码是否正确

**Q3: 传感器数据异常**
- 检查传感器供电是否正常（3.3V）
- 确认GPIO引脚连接正确
- DHT11需要上拉电阻

**Q4: 远程控制不工作**
- 确认设备已订阅`thing/property/set`主题
- 检查JSON格式是否正确：`{"PowerStatus":1,"LightStatus":1}`
- 查看MQTT日志，确认收到远程指令

**Q5: 未刷卡时看不到传感器数据**
- 未刷卡时只上报温湿度和烟雾数据（30秒间隔）
- 查看日志中的"Basic sensor upload"消息
- 在OneNet控制台查看基础传感器数据上报

**Q6: 紧急断电后无法远程恢复**
- 检查紧急模式是否已清除
- 确认远程指令中的用户名处理逻辑
- 查看MQTT日志中的远程控制处理信息

**Q7: 自动控制不工作**
- 确认电源已开启（PowerStatus=1）
- 检查紧急模式标志（Emergency=0）
- 查看ALERT日志中的自动控制检查信息

---

## 📄 许可证

本项目仅供学习和研究使用。

---

## 👨‍💻 作者

ESP32智能工位监控系统

---

## 🔗 相关资源

- [ESP-IDF编程指南](https://docs.espressif.com/projects/esp-idf/zh_CN/latest/)
- [RC522组件文档](https://components.espressif.com/components/abobija/rc522)
- [DHT传感器库](https://github.com/UncleRus/esp-idf-lib)
- [SSD1306显示驱动](https://components.espressif.com/components/espressif/ssd1306)
- [OneNet物联网平台](https://open.iot.10086.cn/)
- [OneNet物模型文档](https://open.iot.10086.cn/doc/art396.html)

## 📚 项目文档

- 📄 `ONENET_TOPIC_GUIDE.md` - OneNet主题使用指南
- 📄 `EDGE_DETECTION_README.md` - 边缘检测系统说明
- 📄 `QUICK_START_GUIDE.md` - 快速上手指南
- 📄 `BACKEND_INTEGRATION_EXAMPLE.md` - 后端集成示例
- 📄 `CHANGES_SUMMARY.md` - 更新摘要

---

## 📮 反馈与支持

如有问题或建议，欢迎提交Issue或Pull Request。

---

**更新日期**：2025-01-XX

---
*最新更新：完全兼容OneNet物模型标准，新增远程控制、无人值守监控、紧急断电保护等功能*
=======
# LEMS-esp32s3
>>>>>>> 42b38e997f1cbf5f8e66773b4b0efbedf47e7d6e
